import React from 'react'

const Brands = ({params}) => {
    console.log(params);
    return (
        <div className="container-fluid">
            hola soy una marca
        </div>
    )
}

export default Brands;